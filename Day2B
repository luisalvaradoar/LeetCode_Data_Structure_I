class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        for i,x in zip(range(m,m+n),nums2):
            nums1[i] = x
        nums1.sort()
        return nums1
